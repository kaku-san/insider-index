<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

# InsiderIndex

User-facing brand is **InsiderIndex** (https://insiderindex.xyz, copy as InsiderIndex.xyz). Repo, package name, `STOCKLANA_*` env keys, module paths, and API routes stay `stocklana`. Consumer FE contract: `docs/insiderindex-fe/FRONTEND-README.md`. Never Chrome.

Disclosure-to-copy-trade app: real insider/politician prints → user-signed xStock swaps on Solana. Product scope, env matrix, and API routes live in `README.md`; empty env template in `.env.example`.

- W0 release: `docs/track-a-w0.md` owns launch checks and receipt semantics. Home (`src/components/consumer-home.tsx`) is indexes-first person discovery; primary trading CTA is `/feed` → copy one print. Basket Buy stays unavailable. Copy receipts are not balances/NAV; production requires service-role Supabase. Do not reintroduce party/executive lanes as top-level navigation.
- Consumer UI overlays existing APIs. Keep `src/app/api/**` and fail-closed vault Sign (`src/components/vault-flow.tsx` only signs when prepare returns validated txs; no stub Buy, no Pyth). After a deposit or cash-out signature, VaultFlow and the positions views poll position/pending and show only an observed status (`pending`, `filling`, `shares received`, `cash out settling`, `failed`); do not fake a progress percent or require a reload. Do not edit `scripts/mag7-settle-deposit.mts` for that listen path. Public Live/Invest uses `publicIndexIsLive` (vault identity + `depositsEnabled`); Mag7 is the live vault today. Indexes without a vault stay Research with no fake Invest. Do not replace backend ingest (`src/lib/disclosures`, `src/lib/fmp`, `src/lib/venues`). Native lifecycle client types live in `src/lib/frontend/vault-api.ts`. Positions and detail lead with USDC value via `src/lib/frontend/position-share-copy.ts` (raw mint units, not decimal-shifted share text such as 0.00002). Do not change the on-chain share mint or the keeper settler to fix that display.
- Public Mag7 cycle (`src/lib/index-vaults/public-cycle-policy.ts`): every public connected wallet, including the configured owner, gets a derived journal on the same vault (deterministic operation id, same limits/keeper; never a second configured policy). The configured operation remains private/operator-only. Public cycle HTTP errors return plain `message` and omit `recovery`. Position reads use `?wallet=`.
- FMP holdings-first books/indexes: `src/lib/fmp/README.md` → Supabase-only people/portfolio reads. `holdings-index.ts` builds from the latest saved annual snapshot; `holding-resolution.ts` retains identity evidence separately. Trades are optional, never balances. Owner publication is `supabase/migrations/202609140004_holdings_indexes.sql`; never rewrite annual rows or route FMP through the legacy PTR-netted calculator. Saved FMP top-20 politician profiles: `src/lib/fmp/top-profiles.ts` and `GET /api/politician-profiles`. PelosiTracker current books for the featured 20 are as-of 2026-09-15 and render as the shown current book; FMP annual snapshots stay older evidence; tracker trades are info-only.
- Person portfolios: public `/p/[id]` is compact tabbed `src/components/profile-view.tsx` (Allocation, Moves, About). Person/theme/profile detail share one default Allocation tab (`src/components/index-allocation.tsx`: chart + one expandable legend, Other expands in place); token details show only the holding's exact source `mint` via `src/lib/frontend/linked-token.ts` — never a ticker lookup or the vault/share mint. Invest is on that same view only when `publicIndexIsLive` in `src/lib/frontend/vault-api.ts` (created vault + per-index deposit gate). Signing still fail-closes via `depositIsEnabled` / `VAULT_RELEASE.publicFundsEnabled` — do not flip the release flag. The long PelosiTracker workspace `src/components/fmp-portfolio.tsx` (`FmpPerson`) is not the public profile route. `src/lib/fmp/index-name.ts` owns canonical index/vault names. Never filter a disclosed book by tradability. Legacy basket readiness is `src/lib/fomo/index-readiness.ts`; crowd indexes are `src/lib/fomo/crowd-indexes.ts`.
- Thematic multi-member research indexes (not person clones): `src/lib/thematic/` + `GET /api/thematic-indexes` + `/indexes/idx-theme-*` + consumer home `#themes`. Feed: `thematic-indexes.live.json`. See `src/lib/thematic/README.md`.
- PelosiTracker top-20 handoff: `src/lib/tracker/README.md`. Keep every zip verbatim (`pelositracker-top20-handoff/`, `pelositracker-top20full-handoff/`, person-page source `pelositracker-top20rere-handoff/`; each MANIFEST.json + test guard); photos mirror to `public/tracker/photos/`. Shown book: copy-trade full holdings only for Pelosi 15 and MTG 76; everyone else top 5 + OTHER (label as slice, do not invent tickers). Full ledgers + allTickersTraded where present are info only; nothing is summed and no tracker dollar is a net worth or NAV. FMP is the older annual disclosure. Copy-trade $, politician-API $, and vault NAV are three figures — never mixed. The vault-ready tracker-positions index (`tracker-index.ts`, component `src/components/tracker-index.tsx` at `/indexes/tracker-<bioguide>`) weights xStock preferred, verified Backpack `.US` when there is no xStock, plus observed Raydium USDC pool from `raydium-pools-mainnet.json` (`npm run raydium:snapshot`, never hand-edit). Exit is USDC only; nothing enables invest/exit.
- Native indexes: `src/lib/index-vaults/README.md` owns integration/release status; `adapter-contract.ts` is the app boundary. One Symmetry V3 vault/native share mint per index; no receipt wrapper or purchase-row ownership. Full cycle is `full-cycle.ts` (create → zap-in → mint → keeper rebalance → USDC zap-out); every stage fails closed until it has a receipt. Public Invest Sign remains off; the narrow mainnet Mag7 cash-out rail is owned by `src/lib/index-vaults/README.md`. Public invest is one N-leg in-kind zap (`in-kind-zap.ts`, `jupiter-build.ts`, `index-deposit.ts`): legs/weights come from the DB definition, $10 minimum, Jupiter `GET /swap/v2/build` first and Raydium direct only if that build has no route, user USDC only, in-kind `buyVaultTx`, no auction pairs for the buy. A missing `JUPITER_API_KEY` fails before spend. A missed name is not counted and is not contributed; unspent USDC stays in the wallet. Mag7 still refuses an unroutable amount with "This amount cannot buy Mag7 right now". A complete in-kind basket can mint without the auction; the Mag7 auction settler remains only for a legacy USDC intent (`mag7MintPlan` still mints a partial auction book and does not mint an empty book). Positions and detail show filled legs versus the target (`position-basket.ts`), not the seven-name target as holdings. Before sign, cash-out copy says leftover stocks and USDC may be sent to the wallet; that is not a share refund. After completion, show “Cash out complete — check your wallet”; without a post-redemption balance delta, never present a pre-clear snapshot as a delivery receipt. After a missed cash-out window, leftover USDC and/or stocks redeem as-is to the owner (`keeper-tick.ts` `prepareWithdrawalKeeperStep`); do not throw `CASH_OUT_WINDOW_MISSED_UNSOLD_ASSETS` or keeper side-pay. Vault legs are catalog tokens (`vault-legs.ts`): xStock preferred, verified Backpack `.US` when there is no xStock, never DEX lookalikes. Layout cap 100 tokens (`native-caps.ts`). Legacy index quote/execute are retired; individual copy trades remain separate. Kaku San execution-test create is `/kaku-admin` only (`kaku-san.ts`), a wallet-signed page (connect wallet, pay + sign in Phantom); do not link it from nav/footer/sitemap/home, do not load a keypair on the server, and do not enable public Invest Sign. `/kaku-admin` also hosts the general create for ANY persisted vault definition (`index-vault-create.ts`, `src/components/index-vault-admin.tsx`, `POST /api/vaults/index/*`): it builds from the Supabase definition record (legs/pools/weights read live, cap throws never truncates), reuses every Kaku San guard (deployer-only, Ed25519 signature, discard-refuses-after-broadcast, deactivate Pyth defaults, Raydium-only), writes the create draft to the **durable shared journal** (`create-draft-store.ts`, one Supabase row per index id via service-role RPCs, migration `202609190001` — never a relative `.data/...` path, which does not exist on the deployed read-only filesystem; an unreadable/unwritable journal refuses the create, and a held lease is `RECOVERY_REQUIRED`, released by hand with `recover_insiderindex_vault_create_draft_lock`), shows catalog-vs-tradable coverage plus the per-vault deposit gate before signing, writes `vault_address`/`share_mint` back via `set_insiderindex_vault_address` (migration `202609170002`, runs after the deposits migration), and never opens deposits. Keep both paths selectable; do not delete the fixed Kaku San basket path. After create, convert defaults to Raydium-only zero-target slots: native-required WSOL stays active, USDC inactive (`native-defaults.ts`, `docs/composition-resume.md`). Atomic configuration prepare checks real simulated post-state and resumes matching pending intents; retries preserve the saved vault. Support balances are not investment weights; nonzero accounted WSOL blocks keeper execution pending reconciliation/USDC-conversion proof. The page may show drift but does not sign rebalance. Rebalance is the automated keeper CLI (`kaku-san-rebalance.ts`, `npm run keeper:kaku-san`) with a dedicated hot wallet — not the deployer Phantom. `--dry-run` default; `--execute --keypair PATH` only on the operator machine; never load that key on the server/app; do not force-rebalance; do not enable public Invest Sign. The DB-driven per-index keeper (any real vault, operable by index id) is `keeper-tick.ts` + `npm run keeper:index -- --index <id>` (operator runbook `docs/insiderindex-keeper-tick.md`): reads vault/mint/legs/targets/cap/keeper/fees from `insiderindex_vault_definitions`, no `--vault`/`--share-mint`, no hardcoded basket; same dry-run-default + explicit-`--execute`-AND-shared-eligibility + file-path-keypair contract; refuses uncreated vaults, a leg with no tradable pool, and a wrong/deployer/host/strategy keeper; writes back `last_rebalance_at`/`last_rebalance_result` via RPC `record_insiderindex_vault_rebalance` (`supabase/migrations/202609180001_insiderindex_vault_rebalance_record.sql`), a dry run never recorded as a rebalance. Drift/eligibility stays in the shared `rebalance-eligibility` module — never a second copy. Per-person index mapping + vault-init from the captain's FMP **annual-holdings** drop (source of truth; distinct from `src/lib/tracker/` PelosiTracker positions) lives in `src/lib/index-vaults/person-index-map.ts` & siblings (README `## Person index mapping`): bucket `data/insiderindex-source-buckets/pelositracker-fmp-latest-top20/` (versioned by source sha256), keeper targets persist in `supabase/migrations/202609160001_insiderindex_vault_definitions.sql`, dry-run `npm run indexes:map:top20`. Cap is 100 legs (throws, never truncates); txn-derived books never become weights; pool evidence is the landed `raydium-pools-mainnet` snapshot (`npm run raydium:snapshot`, fail-closed freshness guard) and the dry-run separates catalog coverage from the tradable coverage the create decision rests on. The snapshot candidate set must cover every mint the definitions can reference — tracker top-holdings + FMP person-book legs + **thematic constituents** (`deriveAllThematicIndexes`), or a thematic-only leg (e.g. ORCL on silicon-hill) is marked untradable purely because it was never probed. All 20 published indexes (10 person + 10 thematic) share ONE derivation: the 10 thematic research baskets run through `buildWeightedIndexDefinition` via `thematic-index-map.ts` (`kind: "thematic"`, never person provenance) — do not fork a parallel thematic vault path. `buildWeightedIndexDefinition` collapses same-company share-class duplicates (Alphabet GOOGL+GOOG etc., curated in `share-classes.ts`) into one leg — the tradable xStock survivor, merged weight, recorded on `provenance.collapsedShareClasses` — so a basket never carries one company twice (a tradable xStock + an illiquid Backpack duplicate); never re-add both classes to a feed/book expecting two legs. Deposits are a **separate per-vault gate** from creation: `depositGate`/`depositsEnabled` (closed by default, opens only at full tradable coverage) with `VAULT_RELEASE.publicFundsEnabled` authoritative on top (`deposits.effectiveEnabled`); columns + the single-row-read GROUP-BY fix are in `supabase/migrations/202609170001_insiderindex_vault_deposits.sql`.
- **Native `update_token_prices` is Raydium-only; zap/eligibility quotes are Raydium then Jupiter.** `raydium-oracles.ts` rejects `oracle_type: "pyth"`, maps `mint → Raydium pool` (never invent one), and builds `update_token_prices` without the SDK's `updateTokenPricesTx` (it always opens a Hermes client). Zap and keeper eligibility (`vault-prices.ts`, `rebalance-eligibility.ts`) take Raydium first, Jupiter if no pool, and never Pyth/Hermes. Do not call SDK `isRebalanceRequired` (it always hits Hermes). Never add Hermes/Pyth clients, keys or `HERMES_*`/`PYTH_*` env; `tests/raydium-oracles.test.mts` executes the real settlement price builder against fixtures, rejects non-devnet network requests, and verifies the native Raydium instruction and accounts. Devnet settlement: `npm run vault:settle:devnet` (`docs/devnet-vault-settle.md`); cycle gate: `npm run vault:cycle`; receipts in `evidence/vaults/DEVNET_TEST_VAULT.md`.
- **NAV vault is the public invest/cash-out rail** (own-share ERC-4626-style, one signature each way, unaudited): `docs/nav-vault.md` (mainnet/devnet ids, keeper, runbook). Symmetry is retired from public flows: `/api/indexes/[id]/{deposit,withdraw}/prepare`, `/position` and `/api/positions/indexes` serve the NAV vault; public Symmetry cycle/devnet routes return 410. Symmetry code stays in-repo (kaku-admin tooling) — do not re-wire it into public UI/API. Live = the index has an on-chain NAV vault (`/api/nav-vault?ids=`, `navVaultLive`/`navIndexStatus`); kill switches `STOCKLANA_NAV_VAULT_DISABLED` / `NEXT_PUBLIC_NAV_VAULT_DISABLED`. Where older bullets below say Symmetry decides Live/Invest/cash-out, the NAV rule wins. Source `programs/nav-vault` (+ `programs/mock-swap`, devnet only); tests load the committed `programs/bin/*.so` (hash-checked) — after editing Rust rerun `scripts/nav-vault-build.sh`. NAV = free (unreserved) USDC + stock marks, never USDC balance alone. NAV vaults hold each index's **tradable slice** (`src/lib/nav-vault/tradable-slices.json` via `scripts/nav-vault-slices.mts`; captain decision): the disclosed book itself stays unfiltered, and the UI must show `TradableSliceNote` ("Tradable slice: N of M holdings (X% of disclosed weight)" + excluded names).
- **Buy catalog, not an allowlist.** Copyable ⇔ the ticker has a Solana mint in `src/lib/venues/solana-catalog.ts` (live xStocks + Backpack `.US`, xStock preferred; snapshot fallback via `npm run catalog:snapshot`). Ondo/Superstate/PreStocks are excluded on purpose (README "Out of V1"). Never hand-add a mint.
- PTRs are dollar bands: keep `amountLow`/`amountHigh` nullable, render ranges, never `$0`. Return / hit rate stay null until a real price series exists.

## Commands

- `npm run dev` / `npm run build` / `npm run typecheck`
- `npm test` — `tests/*.test.mts`; keep `*-parse.ts` free of `@/` aliases, env, and fetch.
- Validation is code-level and offline only. NEVER launch a browser, headless Chrome/Chromium, a dev-server screenshot, or any DOM dump to validate a change (including `/kaku-admin`); a scenario that would need one is untested-with-reason. The no-mistakes Test gate is pinned to `npm run typecheck && npm test` in `.no-mistakes.yaml` so it runs deterministically and never improvises a browser.

## Data sources (`src/lib/disclosures/`)

- Insiders: SEC EDGAR primary (`edgar.ts`), Form4API fallback, mocks dev-only.
- Congress: AInvest primary (`ainvest.ts`, `AINVEST_API_KEY`), Form4API fallback, mocks dev-only. AInvest is ticker-scoped (no per-member pull): the crawl universe is `buildCongressUniverse()` in `universe.ts` (`AINVEST_UNIVERSE`).
- `GET /api/disclosures` → `lanes` tells you what actually served; `catalog` says whether venue tags came from the live catalog or the snapshot. The feed body is served from the committed PelosiTracker/FMP bundle via `src/lib/tracker/feed.ts` (through the tracker handoff module, not a second parse) as research-only rows (`source: "pelositracker"`, `tradeEligible: false`, value bands never exact prices); the live EDGAR/AInvest lanes are reported `off` with provenance rather than probed/retried. Payload is paginated (`?page`/`?limit`, documented default/max in `feed.ts`).

## Sharp edges

- Shared caches go through `globalState()` in `src/lib/cache.ts`.
- EDGAR cold crawl ~30 s; warmed at boot via `src/instrumentation.ts`.
- A stale gitignored `.next/` makes `npm run typecheck` fail on deleted routes (`TS2307 … src/app/api/<removed>/route.js` in `.next/types/validator.ts`). It is a cache, not a source error: `rm -rf .next` (or `npm run build`) and re-run. The no-mistakes Test gate runs `npm run typecheck && npm test` (`.no-mistakes.yaml`), so clear it there before trusting a red typecheck.
- Jupiter live vs stub is `jupiterMode()` in `src/lib/runtime.ts`; live mode rejects stub signatures. Wallet fallback (`src/components/providers/privy-provider.tsx`) fails closed in production; a Privy load failure must never enable a fixture wallet.

## Maintaining this file

Keep this file for knowledge useful to almost every future agent session in this project.
Do not repeat what the codebase already shows; point to the authoritative file or command instead.
Prefer rewriting or pruning existing entries over appending new ones.
When updating this file, preserve this bar for all agents and keep entries concise.
