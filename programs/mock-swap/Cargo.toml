[package]
name = "mock-swap"
version = "0.1.0"
edition = "2021"
description = "Devnet/test-only fixed-price swap venue standing in for Jupiter (Jupiter has no devnet deployment). Never mainnet."

[lib]
crate-type = ["cdylib", "lib"]
name = "mock_swap"

[features]
no-entrypoint = []

[dependencies]
solana-program = "2.2"

[lints.rust]
unexpected_cfgs = { level = "allow" }
